import Component from '@ember/component';
import { inject } from '@ember/controller';

export default Component.extend({
  // addController: inject('rentals.add'),
  // setupController(model, controller) {
  //   let page = addController.get('page');
  //   console.log(page);
  // }
});
